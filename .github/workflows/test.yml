name: Build
on: [push]
jobs:
  build:
    name: Promote
    runs-on: ubuntu-latest
    steps:
      - uses: deployix/deployed-github-actions@main
        if: always()
        with:
          githubToken: ${{ secrets.REPO_GITHUB_SECRET }}
          githubUsername: "deployed-ci"
          description: "test description"
          promotionName: "local-to-dev"
